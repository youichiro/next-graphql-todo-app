### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Mutation {
  createProject(name: String!, userId: Int!): Project!
  createTask(description: String!, projectId: Int!, title: String!): Task!
  createUser(email: String, name: String!): User!
  updateTask(description: String!, done: Boolean!, id: Int!, title: String!): Task!
  upsertSelectedProject(projectId: Int!, userId: Int!): SelectedProject!
}

type Project {
  id: Int!
  name: String!
  tasks: [Task]!
  user: User!
}

type Query {
  projects(userId: Int!): [Project]!
  selectedProject(userId: Int!): SelectedProject
  tasks(projectId: Int!): [Task]!
  users: [User]!
}

type SelectedProject {
  id: Int!
  project: Project!
  user: User!
}

type Task {
  description: String!
  done: Boolean!
  id: Int!
  project: Project!
  title: String!
}

type User {
  email: String
  id: Int!
  image: String
  name: String!
  projects: [Project]
}
